# DesktopPlus — What’s New

## Version 2.1.1

1. Refresh now works reliably.
2. Fixed the tray icon freezing or becoming unresponsive.
3. Fixed the dashboard sometimes looking shifted or scrolled after a refresh.
4. The window no longer jumps in front of other programs when it reloads or repositions.
5. Log files are cleaned up automatically so they do not grow forever.
6. Same features as version 2.0.
7. Upgrading keeps your saved URL if you keep (or copy) `config.json` — no need to type it again.

## Version 2.1

Same reliability work as 2.1.1 (first reliability release after 2.0).

## Version 2.0

1. Choose which screen to use (no typing coordinates).
2. Leave the taskbar visible, or use full-screen kiosk mode.
3. Settings in the system tray.
4. Auto-refresh and scroll options.
5. Works with different screen sizes and scaling.

## Version 1.0

1. Original simple script (`HAD-PLus.pyw`).
